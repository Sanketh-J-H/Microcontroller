#include "uart1ReadWrite.h"


int main()
{

	char c;
    uart1_init();
    gpio_init();
    void gpio_set(void);
    void gpio_reset(void);
    while(1)
    {
    	c = uart1_read();
    	if(c=='N')

    	uart1_write(c);
    }
    return 0;
}
